# Training configuration — read by config_loader as cfg.training.*
# No top-level wrapper key.

test_size: 0.20
random_state: 42
stratify: true
cv_folds: 5
cv_scoring: "roc_auc"
model_output_dir: "models/"
reports_output_dir: "reports/"
